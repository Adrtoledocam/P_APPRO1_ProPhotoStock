<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ProPhotoStock.Pages.LoginPage"
             Title="LoginPage">
    <VerticalStackLayout Padding="30" Spacing="20">
        
        <Label Text="ProPhotoStock" FontSize="32" HorizontalOptions="Center" />
        <Entry Placeholder="Adresse email" Text="{Binding Email}" />
        <Entry Placeholder="Mot de passe" IsPassword="True" Text="{Binding Password}" />
        <Button Text="Se connecter" Command="{Binding LoginCommand}" />
        <Label Text="Créer un nouveau compte" TextColor="Blue" HorizontalOptions="Center" />

    </VerticalStackLayout>
</ContentPage>